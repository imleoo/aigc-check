# AI生成内容检测工具 - 需求文档

## 项目概述

**项目名称**: AIGC-Check (AI Generated Content Checker)
**技术栈**: Go + Gemini 3 Pro API
**目标**: 开发一套检查AI生成内容和提供修改建议的工具系统

---

## 一、核心功能需求

### 1.1 文本检测功能

#### 1.1.1 十大AI特征信号检测

**Signal 1: 过度强调重要性词汇检测**
- 检测词汇: crucial, pivotal, vital, groundbreaking, revolutionary, profound, significant
- 阈值: ≥3次出现标记为警告
- 输出: 词频统计 + 位置标注

**Signal 2: 三件套句式检测**
- 检测模式: Additionally, Furthermore, Moreover 开头的句子
- 阈值: ≥5个句子使用这些开头
- 输出: 句子位置 + 使用频率

**Signal 3: 虚假范围表达检测**
- 检测模式: "from X to Y" 结构
- 验证逻辑: 检查X到Y是否有连续性
- 示例: "from AI to blockchain" (无连续性) vs "from 1990 to 2000" (有连续性)

**Signal 4: 引用格式异常检测**
- UTM参数检测: `utm_source=chatgpt.com`, `utm_source=openai`
- 幽灵引用标记: `contentReference[oaicite:`, `[oai_citation:`, `turn0search`
- 占位符日期: `2025-XX-XX`, `YYYY-MM-DD`

**Signal 5: 破折号密度检测**
- 统计em dash (—) 使用频率
- 阈值: >5次/千字为警告
- 对比: 人类平均0.5-2次/千字, AI平均5-15次/千字

**Signal 6: Markdown残留检测**
- 检测模式: `##`, `**text**`, `[link](url)`, 列表符号 `- `, `* `
- 上下文分析: 判断是否为有意的Markdown格式

**Signal 7: 表情符号异常检测**
- 检测过度工整的emoji使用模式
- 统计emoji密度和分布规律

**Signal 8: 知识截止日期声明检测**
- 检测短语: "As of my last knowledge update", "根据我最后一次训练数据更新"
- 检测短语: "While specific details are limited in the provided sources"

**Signal 9: 协作式沟通语气检测**
- 检测短语: "I hope this helps", "Let me know if you need", "Dear [X], I hope this message finds you well"
- 检测: 邮件格式残留 (Subject:, Dear, Sincerely)

**Signal 10: 完美主义陷阱检测**
- 语法完美度分析 (100%正确率为可疑)
- 缺乏口语化表达
- 缺乏第一人称视角
- 缺乏情绪表达和自我怀疑

---

### 1.2 评分系统

#### 五维度评分模型 (总分100分)

| 维度 | 权重 | 评分标准 | AI典型得分 | 人类典型得分 |
|------|------|----------|------------|--------------|
| 词汇多样性 | 20分 | 词汇重复率、同义词使用 | 12-15 | 16-20 |
| 句式复杂度 | 15分 | 句子长度变化、结构多样性 | 8-10 | 12-15 |
| 个人化表达 | 25分 | 第一人称、个人经历、情感表达 | 5-10 | 18-25 |
| 逻辑连贯性 | 20分 | 段落衔接、论证完整性 | 16-18 | 14-18 |
| 情感真实度 | 20分 | 情绪波动、真实细节、不完美性 | 5-8 | 15-20 |

**风险等级划分**:
- 0-40分: 极高风险 (Very likely AI-generated)
- 41-60分: 高风险 (Likely AI-assisted)
- 61-75分: 中等风险 (Possibly AI-assisted)
- 76-100分: 低风险 (Likely human-written)

---

### 1.3 改写建议功能

#### 1.3.1 基于Gemini的智能建议

**功能描述**: 利用Gemini 3 Pro模型分析问题段落并生成人性化改写建议

**输入**:
- 原始文本
- 检测到的问题类型
- 上下文信息

**输出**:
- 改写建议 (2-3个备选方案)
- 改写理由说明
- 预期改善效果

**建议类型**:
1. 词汇替换建议 (替换AI高频词)
2. 句式重构建议 (改变句子开头和结构)
3. 个人化注入建议 (添加具体细节、数据、个人视角)
4. 情感真实化建议 (添加情绪表达、不确定性)

#### 1.3.2 快速修复建议

**30秒快速自检清单**:
```
□ 文中是否有 ≥3个 "crucial/pivotal/vital"?
□ 是否有 ≥5个 "Additionally/Furthermore/Moreover"开头的句子?
□ 是否有"标志着重要转折点/奠定坚实基础"等套话?
□ 引用链接是否包含 utm_source=chatgpt.com?
□ 是否有"从...到..."但中间没有逻辑连续性?
□ 段落结尾是否总结式重复 (In conclusion...)?
□ 破折号(—)使用是否 >5次/千字?
□ 是否所有语法都完美无瑕疵?
```

---

## 二、技术架构设计

### 2.1 系统架构

```
┌─────────────────────────────────────────┐
│         CLI / Web API Interface         │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│         检测引擎层 (Detection Engine)    │
├─────────────────────────────────────────┤
│  - 规则引擎 (Rule Engine)               │
│  - Gemini API 集成 (AI Analysis)        │
│  - 评分计算器 (Scoring Calculator)      │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│      分析服务层 (Analysis Service)       │
├─────────────────────────────────────────┤
│  - 文本预处理 (Text Preprocessing)      │
│  - 特征提取 (Feature Extraction)        │
│  - NLP处理 (NLP Processing)             │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│       报告生成层 (Report Generator)      │
├─────────────────────────────────────────┤
│  - HTML报告                             │
│  - JSON输出                             │
│  - Markdown报告                         │
└─────────────────────────────────────────┘
```

### 2.2 核心模块设计

#### 2.2.1 规则引擎模块 (Rule Engine)

**职责**: 实现10大信号的检测规则

**接口设计**:
```go
type RuleEngine interface {
    // 检测单个规则
    CheckRule(text string, ruleType RuleType) RuleResult

    // 批量检测所有规则
    CheckAllRules(text string) []RuleResult

    // 获取规则配置
    GetRuleConfig(ruleType RuleType) RuleConfig

    // 更新规则阈值
    UpdateRuleThreshold(ruleType RuleType, threshold float64) error
}

type RuleResult struct {
    RuleType    RuleType
    Passed      bool
    Score       float64
    Issues      []Issue
    Suggestions []string
}

type Issue struct {
    Position    Position  // 行号、列号
    Content     string    // 问题内容
    Severity    Severity  // 严重程度
    Description string    // 问题描述
}
```

#### 2.2.2 Gemini集成模块

**职责**: 调用Gemini 3 Pro进行深度语义分析

**功能**:
1. 语义连贯性分析
2. 情感真实度评估
3. 个人化表达检测
4. 改写建议生成

**接口设计**:
```go
type GeminiAnalyzer interface {
    // 语义分析
    AnalyzeSemantics(text string) SemanticResult

    // 生成改写建议
    GenerateRewriteSuggestions(text string, issues []Issue) []Suggestion

    // 情感分析
    AnalyzeEmotion(text string) EmotionResult

    // 个人化程度评估
    AssessPersonalization(text string) PersonalizationScore
}
```

#### 2.2.3 评分计算模块

**职责**: 计算五维度评分和总分

**接口设计**:
```go
type ScoreCalculator interface {
    // 计算总分
    CalculateTotalScore(results []RuleResult, geminiResult GeminiResult) Score

    // 计算单个维度分数
    CalculateDimensionScore(dimension Dimension, data interface{}) float64

    // 获取风险等级
    GetRiskLevel(totalScore float64) RiskLevel
}

type Score struct {
    TotalScore          float64
    VocabularyDiversity float64  // 词汇多样性
    SentenceComplexity  float64  // 句式复杂度
    Personalization     float64  // 个人化表达
    LogicalCoherence    float64  // 逻辑连贯性
    EmotionalAuthenticity float64 // 情感真实度
    RiskLevel           RiskLevel
}
```

---

## 三、实现优先级

### MVP阶段 (第一版 - 2周)

**目标**: 实现基础检测和评分功能

**功能清单**:
- [x] 项目初始化和架构搭建
- [ ] 规则引擎实现 (10大信号检测)
- [ ] 基础评分系统 (简化版5维度)
- [ ] CLI工具 (单文件检测)
- [ ] 文本格式报告输出
- [ ] 单元测试覆盖

**技术要点**:
- 使用Go标准库进行文本处理
- 正则表达式实现规则匹配
- 简单的评分算法

---

### 第二阶段 (增强版 - 3周)

**目标**: 集成Gemini API和改写建议

**功能清单**:
- [ ] Gemini API集成
- [ ] 深度语义分析
- [ ] 智能改写建议生成
- [ ] HTML报告生成 (高亮问题段落)
- [ ] Web API服务 (RESTful)
- [ ] 配置文件支持 (自定义阈值)

**技术要点**:
- Gemini API客户端封装
- Prompt工程优化
- HTML模板引擎
- HTTP服务框架 (Gin/Echo)

---

### 第三阶段 (完整版 - 4周)

**目标**: 批量处理和高级功能

**功能清单**:
- [ ] 批量文件处理
- [ ] 目录扫描和递归检测
- [ ] 性能优化 (并发处理)
- [ ] 缓存机制 (避免重复分析)
- [ ] 数据库支持 (历史记录)
- [ ] Web界面 (可选)
- [ ] 编辑器插件 (VS Code)

**技术要点**:
- Goroutine并发处理
- Redis缓存
- SQLite/PostgreSQL存储
- WebSocket实时通信

---

## 四、数据结构设计

### 4.1 核心数据结构

```go
// 检测请求
type DetectionRequest struct {
    Text     string            `json:"text"`
    Options  DetectionOptions  `json:"options"`
}

type DetectionOptions struct {
    EnableGemini    bool              `json:"enable_gemini"`
    RuleThresholds  map[string]float64 `json:"rule_thresholds"`
    OutputFormat    string            `json:"output_format"` // text, json, html
    Language        string            `json:"language"`      // zh, en
}

// 检测结果
type DetectionResult struct {
    Score           Score             `json:"score"`
    RuleResults     []RuleResult      `json:"rule_results"`
    GeminiAnalysis  *GeminiResult     `json:"gemini_analysis,omitempty"`
    Suggestions     []Suggestion      `json:"suggestions"`
    Summary         string            `json:"summary"`
    ProcessTime     time.Duration     `json:"process_time"`
}

// 改写建议
type Suggestion struct {
    Type        SuggestionType `json:"type"`
    Original    string         `json:"original"`
    Suggested   []string       `json:"suggested"` // 多个备选方案
    Reason      string         `json:"reason"`
    Impact      string         `json:"impact"`
    Position    Position       `json:"position"`
}
```

---

## 五、API接口设计

### 5.1 RESTful API

#### 5.1.1 文本检测接口

**POST /api/v1/detect**

请求体:
```json
{
  "text": "待检测的文本内容...",
  "options": {
    "enable_gemini": true,
    "output_format": "json",
    "language": "zh"
  }
}
```

响应:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "score": {
      "total_score": 58.5,
      "vocabulary_diversity": 12.0,
      "sentence_complexity": 9.0,
      "personalization": 8.0,
      "logical_coherence": 16.5,
      "emotional_authenticity": 13.0,
      "risk_level": "high"
    },
    "rule_results": [...],
    "suggestions": [...],
    "summary": "检测到多处AI生成特征..."
  }
}
```

#### 5.1.2 批量检测接口

**POST /api/v1/detect/batch**

请求体:
```json
{
  "files": ["file1.txt", "file2.md"],
  "options": {...}
}
```

#### 5.1.3 改写建议接口

**POST /api/v1/rewrite**

请求体:
```json
{
  "text": "原始文本",
  "issue_type": "high_frequency_words",
  "context": "上下文信息"
}
```

---

## 六、CLI工具设计

### 6.1 命令行接口

```bash
# 基础检测
aigc-check detect -f input.txt

# 指定输出格式
aigc-check detect -f input.txt -o html

# 启用Gemini分析
aigc-check detect -f input.txt --gemini

# 批量检测
aigc-check detect -d ./documents --recursive

# 生成改写建议
aigc-check rewrite -f input.txt --output suggestions.md

# 配置管理
aigc-check config set threshold.crucial 5
aigc-check config show
```

### 6.2 输出示例

```
=== AIGC-Check Analysis Report ===
Overall Score: 58/100 ⚠️ (High Risk - Likely AI-assisted)

Red Flags:
- "crucial" appears 7 times (threshold: 3) [Line 12, 45, 67...]
- "Additionally" used 12 times as sentence starter
- No first-person pronouns detected
- Sentiment variance: 0.12 (expected: >0.5)
- UTM parameter detected: utm_source=chatgpt.com [Line 89]

Dimension Scores:
- Vocabulary Diversity: 12/20 ⚠️
- Sentence Complexity: 9/15 ⚠️
- Personalization: 8/25 🚨
- Logical Coherence: 16.5/20 ✓
- Emotional Authenticity: 12.5/20 ⚠️

Suggestions:
1. Replace template phrases in paragraphs 2, 5, 8
2. Add personal examples/anecdotes
3. Vary sentence starters (avoid "Additionally")
4. Include specific data/parameters
5. Add emotional expressions or uncertainties

Detailed Report: report_20260127_143022.html
```

---

## 七、配置文件设计

### 7.1 配置文件格式 (YAML)

```yaml
# aigc-check.yaml

# Gemini API配置
gemini:
  api_key: "YOUR_API_KEY"
  model: "gemini-3-pro"
  timeout: 30s
  max_retries: 3

# 规则阈值配置
thresholds:
  high_frequency_words:
    crucial: 3
    pivotal: 3
    vital: 3
  sentence_starters:
    additionally: 5
    furthermore: 5
    moreover: 5
  em_dash_density: 5.0  # per 1000 words

# 评分权重配置
scoring:
  vocabulary_diversity: 20
  sentence_complexity: 15
  personalization: 25
  logical_coherence: 20
  emotional_authenticity: 20

# 输出配置
output:
  default_format: "text"  # text, json, html
  highlight_issues: true
  show_suggestions: true
  language: "zh"  # zh, en

# 性能配置
performance:
  max_concurrent: 5
  cache_enabled: true
  cache_ttl: 3600  # seconds
```

---

## 八、测试策略

### 8.1 测试数据集

**需要准备的测试样本**:
1. 纯AI生成文本 (ChatGPT, Claude, Gemini)
2. 人类原创文本
3. AI辅助后人工修改的文本
4. 不同领域的文本 (学术论文、技术文档、新闻报道)
5. 中英文混合文本

### 8.2 测试指标

- 准确率 (Accuracy): 正确识别的比例
- 精确率 (Precision): 识别为AI的样本中真正是AI的比例
- 召回率 (Recall): 所有AI样本中被正确识别的比例
- F1分数: 精确率和召回率的调和平均
- 误判率: 人类文本被误判为AI的比例

**目标指标**:
- 准确率 > 85%
- 精确率 > 80%
- 召回率 > 80%
- 误判率 < 15%

---

## 九、部署方案

### 9.1 部署方式

**方式一: 单机部署**
- 编译为二进制文件
- 直接运行CLI工具
- 适合个人使用

**方式二: 服务化部署**
- Docker容器化
- Kubernetes编排
- 负载均衡
- 适合团队/企业使用

**方式三: Serverless部署**
- AWS Lambda / Google Cloud Functions
- API Gateway
- 按需计费
- 适合低频使用场景

### 9.2 Docker部署

```dockerfile
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY . .
RUN go build -o aigc-check ./cmd/aigc-check

FROM alpine:latest
RUN apk --no-cache add ca-certificates
WORKDIR /root/
COPY --from=builder /app/aigc-check .
COPY --from=builder /app/config.yaml .
EXPOSE 8080
CMD ["./aigc-check", "serve"]
```

---

## 十、未来扩展方向

### 10.1 短期扩展 (3-6个月)

1. **多语言支持**: 支持更多语言的检测
2. **自定义规则**: 允许用户添加自定义检测规则
3. **历史对比**: 对比同一作者的历史文本风格
4. **实时检测**: 编辑器插件实时检测
5. **团队协作**: 多用户、权限管理

### 10.2 长期扩展 (6-12个月)

1. **机器学习模型**: 训练专用的AI检测模型
2. **风格迁移**: 自动将AI文本转换为人类风格
3. **多模态检测**: 支持图片、代码的AI生成检测
4. **API市场**: 提供SaaS服务
5. **移动端应用**: iOS/Android应用

---

## 十一、项目里程碑

### Phase 1: MVP (Week 1-2)
- [ ] 项目初始化
- [ ] 规则引擎实现
- [ ] 基础评分系统
- [ ] CLI工具
- [ ] 单元测试

### Phase 2: 增强版 (Week 3-5)
- [ ] Gemini API集成
- [ ] 改写建议功能
- [ ] Web API服务
- [ ] HTML报告生成
- [ ] 集成测试

### Phase 3: 完整版 (Week 6-9)
- [ ] 批量处理
- [ ] 性能优化
- [ ] 缓存机制
- [ ] Web界面
- [ ] 文档完善

### Phase 4: 发布 (Week 10)
- [ ] 性能测试
- [ ] 安全审计
- [ ] 用户文档
- [ ] 部署上线
- [ ] 监控告警

---

## 十二、风险与挑战

### 12.1 技术风险

1. **Gemini API限制**:
   - 风险: API调用频率限制、成本问题
   - 缓解: 实现缓存机制、本地规则优先

2. **检测准确性**:
   - 风险: 误判率过高影响用户体验
   - 缓解: 持续优化规则、收集反馈数据

3. **性能瓶颈**:
   - 风险: 大文本处理速度慢
   - 缓解: 并发处理、分块分析

### 12.2 业务风险

1. **用户接受度**: 需要教育用户正确使用工具
2. **法律合规**: 确保不侵犯用户隐私
3. **竞争压力**: 市场上已有类似工具

---

## 十三、成功指标

### 13.1 技术指标

- 检测准确率 > 85%
- 单文本处理时间 < 3秒
- API响应时间 < 500ms
- 系统可用性 > 99.9%

### 13.2 用户指标

- 用户满意度 > 4.0/5.0
- 日活跃用户 > 1000
- 用户留存率 > 60%
- NPS评分 > 50

---

## 附录

### A. 参考资源

1. **开源项目**:
   - blader/humanizer
   - hardikpandya/stop-slop

2. **在线工具**:
   - GPTZero
   - Originality.AI
   - ZeroGPT

3. **学术资源**:
   - Wikipedia: Signs of AI writing
   - ACL Anthology: AI Detection研究

### B. 技术栈详细说明

**后端**:
- Go 1.21+
- Gin/Echo (Web框架)
- Gemini Go SDK

**存储**:
- SQLite (开发环境)
- PostgreSQL (生产环境)
- Redis (缓存)

**部署**:
- Docker
- Kubernetes
- GitHub Actions (CI/CD)

**监控**:
- Prometheus
- Grafana
- Sentry (错误追踪)

---

**文档版本**: v1.0
**创建日期**: 2026-01-27
**最后更新**: 2026-01-27
**维护者**: 开发团队
